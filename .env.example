DATABASE_URL=postgresql://lawsforum_web:lawsforum_web_dev@localhost:5432/lawsforum
ADMIN_PASSWORD=<something>
DEFAULT_AUTHOR_PASSWORD=<something-else>

# Used to send "become an author" verification/approval/rejection emails (lib/mailer.ts)
SMTP_HOST=<smtp host>
SMTP_PORT=587
SMTP_USER=<smtp username>
SMTP_PASS=<smtp password>
SMTP_FROM=<from address, e.g. "LawsForum <no-reply@lawsforum.example>">
